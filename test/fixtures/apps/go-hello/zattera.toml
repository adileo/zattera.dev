# Fixture app config: Dockerfile build, one HTTP port, healthcheck.
[app]
name = "hello"

[build]
type = "dockerfile"

[deploy]
healthcheck = { path = "/healthz", timeout = "5s" }

[env.production]
min_replicas = 1
max_replicas = 2

[env.staging]
min_replicas = 1
max_replicas = 1
